{
  "name": "rc-car-race",
  "version": "0.1.0",
  "private": true,
  "description": "RC car racing — read the Virtual RC (VRC) USB dongle on macOS / browser (WebHID)",
  "type": "module",
  "scripts": {
    "web": "node web/server.cjs",
    "monitor": "node src/monitor.js",
    "read": "node src/read-raw.js",
    "list": "node src/list.js",
    "descriptor": "node src/hid-descriptor.js"
  },
  "dependencies": {
    "node-hid": "^2.2.0",
    "usb": "^2.11.0"
  }
}
